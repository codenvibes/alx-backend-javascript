/**
 * AUTH: bugsnvibes
 * Creates a Set from an array.
 * @param {Array} arr - The array from which to create the Set.
 * @returns {Set} - A Set containing the elements of the input array.
 */

export default function setFromArray(arr) {
  return new Set(arr);
}
